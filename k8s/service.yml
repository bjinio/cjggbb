apiVersion: v1
kind: Service
metadata:
  name: ddhtfjh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ddhtfjh"
    port: 8080
  selector:
    app: ddhtfjh
